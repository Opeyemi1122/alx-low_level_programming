#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _abs - computes the absolute value of an integer
 * @a: gives integer value
 * Return: Always 0 (Success)
 */
int _abs(int n)
{
	return abs(n);
}
