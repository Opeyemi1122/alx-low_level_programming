#include <stdio.h>

/**
 * main -Entry point of the program.
 *
 * The program prints a line using the print function.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
