#include <stdio.h>

/**
 * main -Entry point of the program
 *
 * The program prints a line using the printf function
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char gramm1 = 'A';

	printf("with proper grammar, but the outcome is a piece of art,%c\n", gramm1);
	return (0);
}
